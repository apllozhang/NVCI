# NVCI 多品牌、产品线与型号级来源配置操作说明

## 适用范围

本功能用于在 NVCI 中登记新的公开官方资料自动镜像来源。配置颗粒度是“品牌 → 产品线 → 子系列 → 具体型号/资料条目”。一条资料条目可以覆盖一个或多个型号；例如同一份官方 Data sheet 覆盖 24 口和 48 口 SKU 时，应在同一条目中填入全部精确型号。

> 来源配置中心不是自由抓取工具。只有已登记的官方 HTTPS 域名、经样本检查验证为公开 PDF 的资料，才能批准进入 NAS 本地自动镜像队列。

## 新建来源的步骤

1. 登录 `http://10.10.10.218:8787`，从左侧导航打开“更新中心”。
2. 在“品牌 / 产品线 / 型号”区域点击“新建来源”。
3. 输入品牌标识和名称。例如标识填写 `hpe`，名称填写 `HPE Networking`。
4. 输入产品线名称和资料库品牌根目录。例如产品线为“交换机”，根目录为“HPE Networking彩页”。
5. 输入子系列，例如“Aruba CX 6300”。一个来源配置建议只对应一个子系列；如需采集 CX 6200，应新建独立来源。
6. 输入官方域名白名单。只填主机名，不填协议、路径或通配符，例如 `www.hpe.com, hpe.com`。
7. 在“型号与官方资料”中填写资料条目。每条必须包含覆盖型号、官方 PDF URL、官方文件名和证据规则；官方产品页可选，但推荐填写以保留资料发现链。
8. 点击“保存为草稿”。草稿不会自动下载，也不会写入活动资料库。

## 审核与启用

保存草稿后，先点击“执行 1–5 条样本检查”。NVCI 对最多 5 条已登记 PDF 进行低频 HEAD 验证，检查 HTTPS 域名、HTTP 状态、Content-Type、ETag、最后修改时间和长度。

| 样本检查结果 | 下一步 |
|---|---|
| 全部通过 | 点击“批准并启用”。此时来源允许手动运行或设置计划。 |
| 有任一失败 | 保持草稿，修正域名、官方 URL、文件名或来源策略后重新样本检查。 |
| 已批准 | 点击“立即运行”执行首次镜像，或启用每周计划。 |
| 已暂停 | 不再自动运行；点击“编辑并重新验证”后才能恢复。 |

批准不会自动下载 PDF。首次镜像必须由管理员点击“立即运行”或等到已启用的计划触发，以避免未知来源在未经确认时消耗网络带宽和存储空间。

## NAS 保存路径与版本规则

首次镜像通过后，当前有效 PDF 将保存到：

```text
/vol2/1000/NVCI/library/<资料库品牌根目录>/<产品线>/01 官方彩页/<子系列>/<官方文件名>
```

每次运行的五列表格、路径健康日志、哈希清单、变更日志、运行结果和不可变 manifest 将保存到：

```text
/vol2/1000/NVCI/library/<资料库品牌根目录>/<产品线>/08 更新与缺口记录/<日期_运行标识>/
```

已发布快照不得覆盖。发生 PDF 内容变化时，系统使用 SHA-256 创建新的快照，同时以原子方式替换活动资料库中的当前有效 PDF；未变化资料仅复用现有元数据与哈希，不重复下载。

## 示例：配置 HPE Aruba CX 6300

| 字段 | 示例 |
|---|---|
| 品牌标识 | `hpe` |
| 品牌名称 | `HPE Networking` |
| 产品线 | `交换机` |
| 子系列 | `Aruba CX 6300` |
| 官方域名 | `www.hpe.com, hpe.com` |
| 型号 | `JL658A, JL659A` |
| 证据规则 | `official_datasheet` |

URL 必须先依据厂商官方资料规则核验。对于 HPE 产品，以官方 Data sheet 为主；如需商城素材补充，应按 Data sheet、Specifications、QuickSpecs 的顺序登记。

## 常见问题

**Q：同一彩页覆盖多个型号怎么办？**

将全部型号填在同一资料条目的“覆盖型号”中，以逗号分隔。不要为了同一 PDF 创建多条重复 URL。

**Q：可以直接把任意搜索结果 URL 填入吗？**

不可以。URL 主机名必须精确匹配配置中的官方域名白名单，并通过公开 PDF 样本检查。

**Q：修改已批准来源会影响已下载资料吗？**

不会删除历史资料，但修改后会自动回到草稿状态并暂停后续自动运行。必须重新样本检查和批准，避免未经复核的 URL 替换既有资料。

**Q：是否能自动发现全产品线？**

当前版本自动镜像已登记的资料条目；它不进行宽泛全站扫描。全产品线发现应先根据厂商官网规则建立受控清单，再在此界面审核启用。
