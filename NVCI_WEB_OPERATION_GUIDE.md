# NVCI Web 工作台操作手册

> 适用地址：`http://10.10.10.218:8787`
>
> NAS 持久化资料根目录：`/vol2/1000/NVCI/`
> 容器内资料库根目录：`/data/library/`（映射到 NAS 的 `/vol2/1000/NVCI/library/`）

## 一、先理解当前版本的使用边界

NVCI 当前版本是一个**本地资料治理、审计与受控自动采集工作台**。它用于保存厂商采集记忆、查看产品彩页状态、浏览 NAS 活动资料库、做低消耗路径健康检查、运行已登记来源的自动增量采集、保留任务日志和导出当前状态。

> “运行路径健康检查”只会读取有限样本 URL 的 HTTP 状态、日期、长度、ETag 等元数据；**不会下载 PDF，也不会自动开始全量采集**。已登记的 **ALE OmniSwitch** 来源应在“更新中心”通过 NAS 自动采集器运行；尚未登记的 HPE、Cisco、H3C、锐捷或其他新产品线，仍需先经过官方来源审核后再加入自动采集配置。

当前版本仍**未提供**网页端的 PDF 上传、CSV 批量导入或 PDF 在线预览；但已提供 NAS 本地后台自动采集器。自动采集器只处理“更新中心”中已登记的公开官方资料来源，不能从网页提交任意 URL，也不会登录或绕过厂商访问控制。

## 二、登录与首页检查

### 步骤 1：打开工作台

在与 NAS 同一局域网的电脑浏览器中访问：

```text
http://10.10.10.218:8787
```

出现“本地管理员登录”后，输入**首次部署时设置的 NVCI 管理员密码**。不要将密码发到聊天、写入资料清单或提交到 GitHub。

### 步骤 2：检查控制台摘要

登录后默认进入“控制台”。请先确认以下卡片数据能正常显示：

| 卡片 | 含义 | 当前部署的预期初始值 |
|---|---|---:|
| 厂商路径记忆 | 已维护的官网采集规则数量 | 5 |
| 产品/系列状态 | 已登记的产品或系列资料状态 | 8 |
| 已获公开 PDF | 已确认获得公开官方 PDF 的记录数 | 3 |
| 用户确认无彩页 | 经人工确认没有彩页的记录数 | 5 |
| 归档文档示例 | 已登记的资料库文档记录数 | 3 |

上述数字会随资料导入和状态维护变化。重点是卡片、厂商健康状态和最近任务能正常载入。

## 三、逐页操作说明

### 1. 控制台

控制台用于查看全局状态和启动一次低消耗检查。

1. 查看“厂商路径健康”区域，确认各厂商显示“健康”或“已验证”。
2. 点击右侧的 **“运行路径健康检查”**。
3. 等待提示完成；系统会对已配置的健康检查 URL 做 HEAD 请求。
4. 在“最近任务”查看本次结果。若显示 `completed`，说明样本 URL 的元数据可正常读取；若显示 `attention`，请进入“任务日志”确认哪一个厂商需要重新验证采集路径。

建议在开始一次正式增量采集前先运行一次健康检查。常规资料库无紧急更新时，每周一次即可；不要反复点击以替代正式采集。

### 2. 厂商记忆

“厂商记忆”用于维护每个厂商的采集策略和小样本检查 URL，而不是保存账号密码或绕过访问限制。

1. 点击左侧 **“厂商记忆”**。
2. 找到 **ALE** 卡片。
3. 核对“主证据”是否仍为 **官方 Data sheet 与 Order information**。
4. 核对“采集路径与访问边界”：产品页用于发现；Data sheet 和其中的 Order information 为型号主证据；PDF 优先。
5. 必要时只修改两项：
   - “采集路径与访问边界”；
   - “小样本健康检查 URL”。
6. 点击 **“保存厂商记忆”**。

> 请只填写公开官方 URL。不要在该页面填写 NAS、Google Drive、GitHub 或厂商网站的密码、令牌、Cookie、签名下载链接或登录信息。

### 3. 产品与彩页

“产品与彩页”用于浏览和筛选已登记的产品资料状态。

1. 点击左侧 **“产品与彩页”**。
2. 使用关键字搜索框查找 `ALE`、`OmniSwitch`、系列名或型号。
3. 使用状态筛选器查看“已获公开 PDF”“待复核”“资源 404”“访问受限”或“用户确认无彩页”等记录。
4. 点击“产品 / 资源页”可回到官方产品页或官方资源页核对来源。
5. 阅读“备注”列，了解该记录的资料来源、人工确认和待办事项。

当前网页端仅提供**查看、筛选和审计**；批量新增产品状态需通过后续导入接口或由采集分析工作流生成规范化数据后执行，不能把此页当作批量下载器。

### 4. 本地资料库

“本地资料库”扫描的是容器内 `/data/library/`，也就是 NAS 上的：

```text
/vol2/1000/NVCI/library/
```

操作步骤如下：

1. 先通过 fnOS 文件管理器或受控 NAS 文件同步，将已经通过 SHA-256 核验的官方 PDF 和清单文件放入资料库。
2. 回到 NVCI 左侧 **“本地资料库”**。
3. 点击 **“重新扫描资料库”**。
4. 查看 PDF、CSV、JSON 和其他文件的数量。
5. 在下方表格查看相对路径、扩展名、文件大小和修改时间，确认文件已被识别。

对于 ALE 交换机，建议使用以下结构：

```text
/vol2/1000/NVCI/library/
└── ALE产品彩页/
    ├── 01 交换机/
    │   ├── 接入层/
    │   │   ├── OmniSwitch 2260/
    │   │   ├── OmniSwitch 2360/
    │   │   ├── OmniSwitch 6360/
    │   │   ├── OmniSwitch 6370/
    │   │   └── OmniSwitch 6560/
    │   ├── 汇聚层/
    │   │   ├── OmniSwitch 6870/
    │   │   ├── OmniSwitch 6900/
    │   │   └── OmniSwitch 6970/
    │   └── 核心层/
    │       └── OmniSwitch 9900/
    ├── 08 更新与缺口记录/
    └── 99 历史归档/
```

活动目录只存放当前有效的官方 PDF、五列表格、`document_manifest.csv`、`collection_baseline.csv`、`path_health_log.csv`、`update_summary.csv` 和必要的 JSON 审计记录。历史 PDF、旧 HTML 和旧快照应移至 `99 历史归档/`，不能覆盖已发布快照。

每个资料快照目录至少放置一个五列表格，列为：**序号、型号/系列、彩页文件名、下载链接 URL、下载时间**。

### 5. 更新中心

“更新中心”用于运行和管理 NAS 本地自动增量采集器。

1. 点击左侧 **“更新中心”**。
2. 在“自动增量采集器”区域确认 ALE OmniSwitch 的受控来源数量、首次镜像状态、后台状态和上次结果。
3. 首次启用时点击 **“立即运行 ALE OmniSwitch”**。按钮只会将任务写入 NAS 本地队列；后台采集器随后执行 15 份已登记的公开官方 Data sheet 的元数据与 SHA-256 核验。
4. 首次镜像成功后，PDF 会自动写入 `/vol2/1000/NVCI/library/ALE产品彩页/01 交换机/01 官方彩页/`，并生成独立不可覆盖快照和五列表格。
5. 未来运行时，只有 URL、ETag、日期、长度或完整哈希发生差异的资料才会下载；无差异时只写入审计结果。
6. 如需修改节奏，设置来源启用状态、每周计划、星期和时间，然后点击 **“保存自动计划”**。默认每周一 `02:15` 运行。

页面中的“执行当前样本检查”仍是轻量级路径检查，不会下载 PDF；它适合在准备调整来源配置前验证公开入口。

### 6. 任务日志

“任务日志”用于审计已经发生的动作。

1. 点击左侧 **“任务日志”**。
2. 检查每一条记录的任务类型、状态、摘要和时间。
3. 健康检查显示 `completed` 时，表示检查完成；显示 `attention` 时，优先查看对应厂商 URL 和决定结果。
4. 当后续接入导入、人工确认或增量下载时，也应以此页面作为最先查看的审计入口。

### 7. 设置与状态导出

“设置”页显示工作台的本地配置摘要。

1. 点击左侧 **“设置”**。
2. 确认“资料库路径”显示容器路径 `/data/library`。这是正确的容器内部路径，其 NAS 对应路径是 `/vol2/1000/NVCI/library/`。
3. 需要备份当前工作台记录时，点击 **“导出状态”**。浏览器会下载 `nvci-state-export.json`。
4. 将该导出文件放入受控备份位置，例如 NAS 的 `99 历史归档/工作台状态导出/` 或另行备份至受控存储；不要覆盖已有时间戳版本。
5. 使用左下角 **“退出”** 结束当前本地管理员会话。

## 四、ALE 交换机彩页的一次标准操作流程

以下步骤将“正式采集”和“NVCI 管理”正确衔接。

### A. 在 NVCI 中做准备

1. 登录 NVCI。
2. 进入“厂商记忆”，核对 ALE 的 Data sheet / Order information / PDF 优先规则。
3. 在“更新中心”或“控制台”运行一次路径健康检查。
4. 在“任务日志”确认检查结果正常。

### B. 在 NVCI 中发起正式自动采集

进入 **更新中心**，在“自动增量采集器”区域点击 **“立即运行 ALE OmniSwitch”**。任务将在 NAS 的 `nvci-collector` 容器中运行，无需打开 Manus、手工下载、校验 ZIP 或上传文件。

自动采集器只访问已登记的 ALE 官方 HTTPS 来源；以 Data sheet 与其中 Order information 为型号主证据。首次运行会完成受控初始镜像；后续运行遵守“路径健康 → 元数据差异 → 仅候选下载 → SHA-256 核验 → 不可变快照”的门禁。

### C. 检查自动入库结果

任务完成后，进入“本地资料库”并点击“重新扫描资料库”。当前有效 PDF 位于：

```text
/vol2/1000/NVCI/library/ALE产品彩页/01 交换机/01 官方彩页/
```

每次运行的五列表格、`document_manifest.csv`、`path_health_log.csv`、`update_summary.csv` 和 `manifest.json` 位于：

```text
/vol2/1000/NVCI/library/ALE产品彩页/01 交换机/08 更新与缺口记录/
```

> 如果 fnOS 文件管理器无法向 `/vol2/1000/NVCI/` 写入，请不要为了方便而将整个目录设为 `777`。应先记录具体的权限报错，再调整 ACL 或所有者权限，确保 NVCI 容器用户和 NAS 管理用户都以最小权限访问资料。

## 五、日常使用节奏

| 频率 | 操作 | 目标 |
|---|---|---|
| 每次正式采集前 | 健康检查、检查厂商记忆 | 验证官网路径和资料优先级仍然有效。 |
| 每次采集后 | 本地资料镜像、SHA-256、清单、资料库扫描 | 让原始 PDF、URL 和审计记录可复核。 |
| 每周或按需 | 更新中心记录检查节奏、任务日志复核 | 发现路径失效或变化候选。 |
| 每月或重大变更后 | 导出状态 JSON、备份 `/vol2/1000/NVCI/` | 保留可恢复的工作台状态与资料库版本。 |

## 六、常见问题

| 现象 | 原因与处理方式 |
|---|---|
| 健康检查完成，但没有新增 PDF | 正常。健康检查本来就不下载资料；只有正式采集工作流才会下载变化候选。 |
| 本地资料库显示为空 | 确认文件是否确实位于 `/vol2/1000/NVCI/library/`，然后点击“重新扫描资料库”。 |
| 设置页显示 `/data/library` | 正常。这是容器路径，对应 NAS 路径 `/vol2/1000/NVCI/library/`。 |
| 资料放入后未显示 | 检查文件权限、扩展名和是否放在 `library/` 下；再重新扫描。 |
| 厂商路径显示待验证 | 到“厂商记忆”核对公开健康检查 URL；不要猜测下载接口或绕过登录限制。 |
| 导出状态文件在哪里 | 下载到使用浏览器的电脑；需自行复制到 NAS 或其他受控备份位置。 |
| 能否在网页里直接开始全量 PDF 抓取 | 可以，但仅限“更新中心”中已经登记的受控公开官方来源。首次支持 ALE OmniSwitch；新厂商或新产品线需先新增并审核来源配置。 |
