# 产品经理技术字段范围治理

## 目的

字段范围治理用于将“本轮研究要回答哪些技术问题”转化为可版本化、可审批、可审计的任务字段包。它只定义字段、优先级和证据门槛；不自动生成参数值，也不以推断代替官方证据。

P0-2.1 的首个模板是 **园区固定配置交换机｜核心技术字段 v1**，用于 ALE OmniSwitch 的园区接入/汇聚产品定型、产品组合覆盖与 Aruba CX 横向对标。该模板可在后续复用于其他固定配置交换机厂商，但字段事实仍必须按各厂商官方资料独立采集和审核。

## 使用流程

| 步骤 | 产品经理操作 | 系统行为 | 结果 |
|---|---|---|---|
| 1 | 初始化或选择研究任务 | 加载该任务的实体范围、现有字段包和审核队列 | 明确“字段定义属于哪项研究” |
| 2 | 选择字段模板 | 显示字段代码、中文名、字段组、类型、单位、优先级、必选标记与证据门槛 | 形成统一产品口径 |
| 3 | 勾选本轮字段并填写范围说明 | 创建 `pending_approval` 任务字段包和高优先级范围审核项 | 不改变技术字段覆盖率口径 |
| 4 | 填写批准依据并批准 | 旧活跃字段包（如有）被标记为 superseded；新版本变为 `active` | 新字段范围成为唯一计算口径 |
| 5 | 受控抽取与人工审核 | 后续字段事实必须按字段的官方证据门槛写入并回链 | 技术字段覆盖率开始反映真实完成度 |

## 证据门槛

字段范围模板中的 `evidenceRequirement` 是下限，而不是宣传建议。对于 ALE，型号和 SKU 以官方 Data sheet 与其中 Order information 为主证据；协议、软件版本、堆叠、OSPF、VXLAN/EVPN 等需要时应优先补充官方配置指南、命令参考或版本说明。任何未披露字段必须以“未披露”状态保存，而不是依据系列定位、相邻型号或第三方资料推断。

## 状态和指标

字段包有 `pending_approval`、`active`、`superseded` 三种状态。只有 `active` 字段包参与技术字段覆盖率的分母计算；待审批范围不会造成覆盖率变化。覆盖率的分母等于研究范围内实体数量乘以已选字段数，分子只计算具有可接受证据、适用范围和审核状态的字段事实。

## 数据边界

字段模板、任务字段包、审核项和治理审计都写入 `/data/intelligence/intelligence.db`。它们不会改写 PDF、活动资料库、受控来源配置、采集 manifest 或已发布快照。可通过“导出情报快照”单独备份实体、证据、字段事实、任务、审核和字段范围版本。

## 首批 ALE 建议字段包

产品经理可先选择所有高优先级字段：硬件形态、下行/上行端口、速率、PoE 支持、交换容量、堆叠/虚拟化、三层路由与 OSPF。PoE 预算、转发率、最大堆叠成员数、VXLAN/EVPN、自动化、网管与 ACL 可根据当前产品定型问题一并纳入，或保留给下一版本字段包。字段范围的选择应由研究问题决定，而不是为了提高“覆盖率”而勾选无关字段。
