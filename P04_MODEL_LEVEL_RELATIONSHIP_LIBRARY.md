# P0-4 型号级对标关系库

## 发布范围

P0-4 将 NVCI 从“字段事实可追溯”推进到“型号级关系可治理”。首批试点仅覆盖 ALE OmniSwitch 6360、6370、6560/E 与 HPE Aruba CX 6100、6200、6300 的**基础技术型号**；国家、电源线、区域及其它商业变体不单独创建重复关系。

导入输入来自随应用发布的 `intelligence/baselines/p04-pilot-input-audit.json`。该审计基线记录六个系列的型号清单、官方 Data sheet 来源 URL、型号级原文摘录、原始 PDF 相对路径、文件大小与 SHA-256，并回链至历史不可变研究快照。执行导入不会修改 PDF、资料库、采集配置、来源配置或历史快照。

## 关系语义

| 关系状态 | 含义 | 是否可直接用于产品定型 |
|---|---|---|
| `direct_candidate` | 已通过首层硬门槛的直接候选。仍需人工审核双方技术字段的适用条件、软件版本和采购场景。 | 否；仅批准后可用。 |
| `partial_candidate` | 核心定位相近但存在明确技术、端口、供电、性能或管理边界差异。 | 否；须连同偏离项使用。 |
| `adjacent_upgrade` | 同一竞争带内的相邻升级或降级候选，不应表示为一对一直接替代。 | 否；仅用于产品地图与升级路径讨论。 |
| `not_comparable` | 已核验硬门槛不满足，例如固定设备与模块化设备、下行介质不同或端口规模不相容。 | 否。 |
| `insufficient_evidence` | 决定性硬门槛的官方型号级证据缺失或仅到系列级。 | 否；必须补证后重审。 |

> “未披露”代表官方资料未披露，绝不等价于“不支持”。“待复核”代表已有候选值但尚缺少可重放的型号级官方原文。两者均不得被转换为产品定型结论。

## 首层门槛与证据

P0-4 首层判定只使用双方均可追溯的基础硬件字段：产品形态、下行介质、下行端口数、下行速率带、上联端口数、上联速率带和 PoE 类别。端口、供电、性能、协议、堆叠、软件版本及授权差异以 `key_deviations` 和采购验证问题保留；不通过静默加权分数掩盖差异。

每一条关系保存：关系状态、审核状态、判定理由、关键偏离、采购验证问题、输入快照 ID、导入审计 ID、双方字段事实和双方官方证据。证据关联包含资料标题、官方 URL、SHA-256、原文摘录和定位信息，因此可以从关系详情回到原始资料修订。

## 使用流程

在 **情报核心（试点）** 中，先点击“预览 P0-4 对标关系”。预览会返回基础技术型号数量、候选对数量、系列覆盖和快照标识，且不写入 SQLite。确认范围后点击“执行受控关系导入”。该操作写入独立 SQLite 情报核心，具备幂等性：重复执行复用相同实体、字段事实、证据和关系，不重复创建记录。

导入后默认显示“直接候选”。产品经理应筛选关系状态与审核状态，打开“查看证据”，核对双方硬门槛、关键偏离、资料 SHA-256、原文定位和采购验证问题。只有填写依据并将关系审核为 `approved` 后，才可将其用于型号级产品定型、组合覆盖或正式竞争结论。

## 首批验收标准

| 验收项 | 标准 |
|---|---|
| 输入边界 | 仅包含六个系列的基础技术型号及六份受控官方资料修订。 |
| 预览 | 返回 81 个型号、1,610 条候选对；不写入 SQLite。 |
| 关系状态 | 必须区分直接候选、部分候选、相邻升级、不宜比较和证据不足。 |
| 审核门禁 | 导入后候选关系默认不视为已批准；未经批准不得用于产品定型结论。 |
| 证据回链 | 每条详情可见双方资料修订、SHA-256、原文与定位；无法重放的字段生成待复核项。 |
| 幂等性 | 连续两次执行不增加重复实体、重复证据或重复关系。 |
| 数据边界 | 不修改 PDF、活动资料库、来源配置、快照或已发布归档。 |

