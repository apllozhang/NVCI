# NVCI v1.2.0 NAS 更新说明

## 本次新增

本次更新新增“新手任务向导”和无模型公开资料采集器。产品经理可通过六步流程建立**采集、整理与归档任务**：定义归档目标、选择一个或多个厂商、选择产品线或系列来源配置、决定立即/一次预约/每日/每周执行、指定关键字段目录并确认门禁。任务详情将显示产品列表、产品页 URL、官方 PDF URL、证据规则、采集队列、来源审批状态和字段目录审批状态。

采集器不会绕过既有来源审批、官方域名白名单、PDF 签名/适用性/SHA-256 门禁，也不会把未披露字段写成不支持。它不调用 AI 模型，不发现任意 URL，不生成竞争对比、型号映射或评分。一次预约任务在触发后会清空下次执行时间，防止后台 Worker 重复排队。

## 更新前检查

确认现有 Compose 项目目录为 `/vol1/1000/docker/nvci` 或实际 NVCI 项目目录。保留 `.env`、`NVCI_DATA_BIND_PATH`、`NVCI_ADMIN_PASSWORD` 与 `NVCI_SESSION_SECRET`；这些文件和数据卷不能被更新包覆盖或删除。

## 更新命令

```bash
cd /vol1/1000/docker/nvci

# 先备份代码与数据卷引用信息；不删除 nvci_data 或宿主机数据目录
tar -czf /vol2/1000/NVCI/backups/nvci-v1.1.0-code-$(date +%F-%H%M).tar.gz \
  --exclude=node_modules --exclude=.git .

# 将 v1.2.0 更新包解压或通过 Git 同步到当前目录后，保留现有 .env
docker compose --progress=plain build nvci nvci-collector
docker compose up -d --no-deps nvci nvci-collector
docker compose ps
```

## 验收

```bash
curl -s http://127.0.0.1:8787/health
docker logs --tail 80 nvci-workbench
docker logs --tail 80 nvci-collector
```

健康接口应包含 `"version":"1.2.0"`。在浏览器打开 `http://NAS_IP:8787/`，左侧应出现“新手任务向导”。使用已批准的 ALE OmniSwitch 来源创建一张即时或每周采集归档任务，详情应能显示 15 个系列级基础产品实体，以及每条官方产品页和 Data sheet URL。

也可在容器中或项目目录运行无模型采集器：

```bash
npm run collect -- --data-dir /data --profile ale_omniswitch --dry-run
npm run collect -- --data-dir /data --profile ale_omniswitch --queue
```

## 回滚

保留旧镜像 `nvci-workbench:1.1.0`。若必须回滚，只将 `docker-compose.yml` 镜像标签改回 `1.1.0`，然后执行：

```bash
docker compose up -d --no-deps nvci nvci-collector
```

不要删除 `/data/onboarding-tasks.json`、`/data/intelligence/intelligence.db` 或资料库。v1.2.0 新增任务卡是附加数据，不会改写既有 PDF、来源配置、不可变快照或字段事实。
