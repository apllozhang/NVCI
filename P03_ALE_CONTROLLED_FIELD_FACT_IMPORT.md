# P0-3：ALE 受控字段事实导入

> 发布版本：**NVCI v0.9.0**  
> 试点对象：**ALE OmniSwitch 15 个系列**  
> 字段范围：`campus_switching_v1` 的 17 个核心技术字段

## 1. 目的与边界

P0-3 将已验证的 ALE OmniSwitch 官方资料中的技术信息写入独立 SQLite 情报核心，使其成为可追溯的**字段事实**，而不是停留在 PDF、清单或报告文本中。导入仅接受随应用版本发布的审计基线 `intelligence/baselines/ale-field-facts-audit-2026-08-20.json`；该基线中的每项事实都保留官方 Data sheet URL、资料标题、引用摘录与页码/表格定位。

该流程不会修改活动资料库中的 PDF、采集来源配置、不可变快照、Google Drive 归档或已发布的采集 manifest。它不会从客户端接收任意文件路径或自由文本数据，也不会以产品定位、相邻型号或第三方资料补齐未披露字段。

## 2. 运行门禁

字段事实导入必须按下列顺序完成。任一步未满足时，预览和执行接口均会拒绝写入。

| 顺序 | 前置条件 | 系统核验 | 目的 |
|---:|---|---|---|
| 1 | ALE 只读导入完成 | 存在 15 个 ALE 系列、官方资料、资料修订和 SHA-256 基线 | 确保字段事实有可回链的受控资料对象 |
| 2 | ALE 治理试点已初始化 | 存在研究任务卡 `ALE OmniSwitch 纵向产品线基线审阅` | 使事实写入受研究任务治理 |
| 3 | 字段范围已批准 | 活跃字段包为 `campus_switching_v1` 且包含 17 个字段 | 防止未批准的字段进入事实层 |
| 4 | 审计基线完整 | 15 个系列 × 17 个字段 = 255 项；系列和字段均与活跃范围一致 | 防止错配、缺项与越界写入 |
| 5 | 显式执行 | 管理员在工作台确认执行 | 防止无感或后台自动写入 |

## 3. 字段范围

| 字段组 | 字段代码 |
|---|---|
| 硬件形态 | `form_factor` |
| 端口与供电 | `downlink_ports`、`downlink_speed`、`uplink_ports`、`uplink_speed`、`poe_support`、`poe_budget` |
| 性能 | `switching_capacity`、`forwarding_rate` |
| 架构与可靠性 | `stacking_virtualization`、`max_stack_members` |
| 协议能力 | `l3_routing`、`ospf_support`、`vxlan_evpn_support` |
| 运维能力 | `automation_api`、`management_platform` |
| 安全能力 | `acl_security` |

## 4. 三种事实状态

覆盖率代表**字段记录已治理地进入事实层**，不代表全部字段均已核验。因此工作台必须同时显示三态分布。

| 状态 | 当前数量 | 含义 | 对对标和决策的处理 |
|---|---:|---|---|
| `evidence_verified` | 223 | 官方资料有明确摘录与定位，且通过审计 | 可作为带条件的官方事实使用 |
| `not_disclosed` | 31 | 当前受控官方资料没有足以确认的明确披露 | 保留“未披露”；不得推断为不支持 |
| `needs_review` | 1 | 证据定位、适用范围或审计结果仍需人工判断 | 自动创建审核队列项；不得进入确定性对标结论 |

## 5. 预览、执行和幂等性

工作台提供两个独立操作。**预览**只校验门禁并显示预计事实数量与状态分布，不写入 SQLite。**执行**在管理员确认后创建或复用字段事实、证据对象和审核项，并将研究任务推进为 `fact_review`。

事实、证据与审核项使用稳定自然键进行去重。相同审计基线的重复执行会记录新的导入审计，但会复用既有 255 项字段事实，而不会产生重复数据。导入结果同时记录在 SQLite 导入运行表与工作台任务日志中。

## 6. 验收标准

| 验收项 | 预期结果 |
|---|---|
| 预览 | 显示 255 项计划字段事实，且不创建任何事实记录 |
| 首次执行 | 新增 255 项字段事实与相应证据对象 |
| 覆盖率 | 技术字段记录覆盖率为 `255/255`（100%） |
| 状态分布 | 已核验 223、未披露 31、待复核 1 |
| 审核队列 | 出现 1 条 `field_fact` 类型待复核项 |
| 幂等性 | 重复执行新增字段事实为 0，复用字段事实为 255 |
| 数据边界 | PDF、来源配置、活动资料库与历史快照均不发生变更 |

## 7. 后续使用约束

P0-4 的型号级对标关系只能直接引用 `evidence_verified` 字段事实；`not_disclosed` 必须作为信息缺口呈现，`needs_review` 必须在审核关闭并形成新的可审计事实前排除在确定性结论之外。若产品经理修改字段范围，必须创建并批准新的字段包版本；原有事实不会被删除，但新覆盖率将严格按新范围重新计算。
